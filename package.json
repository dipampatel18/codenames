{
  "name": "codenames-field-console",
  "version": "2.0.0",
  "description": "Codenames — multiplayer party game. Single-file Node server, zero dependencies, Server-Sent-Events realtime. Runs locally (Termux) or online (Render).",
  "main": "codenames.js",
  "scripts": {
    "start": "node codenames.js"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
